# Disabled GitHub Pages deployment workflow - renamed to prevent running
# Original file renamed to deploy-gh-pages.yml.disabled
# To re-enable, rename back to deploy-gh-pages.yml
# This workflow is disabled because full app deployment requires a different approach
